% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{ou_geom_target}
\alias{ou_geom_target}
\title{Build a geometry-engine sampling target}
\usage{
ou_geom_target(
  object = NULL,
  log_prob = NULL,
  grad_log_prob = NULL,
  dim = NULL,
  hessian = NULL,
  param_names = NULL,
  data = NULL
)
}
\arguments{
\item{object}{Optional cmdstan fit/model (with methods) or a list carrying
\code{log_prob}/\code{grad_log_prob}/\code{dim}.}

\item{log_prob, grad_log_prob}{R closures of the unconstrained parameter vector
(used when \code{object} is \code{NULL}).}

\item{dim}{Integer unconstrained dimension (required for closures or a
cmdstan model).}

\item{hessian}{Optional Hessian closure (used by the SoftAbs Riemannian
metric); finite-differenced from the gradient when absent.}

\item{param_names}{Optional parameter names.}

\item{data}{Stan data list (used when \code{object} is an uncompiled-to-fit
\code{CmdStanModel} that must be sampled once to expose its methods).}
}
\value{
An object of class \code{ou_geom_target}.
}
\description{
Wrap an unconstrained log-density (and its gradient, optionally its Hessian)
into the target object \code{\link{ou_geom_hmc}} integrates. Accepts either a
cmdstan fit/model compiled with \code{compile_model_methods = TRUE} (whose
\code{$log_prob} / \code{$grad_log_prob} / \code{$hessian} act on the
unconstrained scale) or explicit R closures.
}
