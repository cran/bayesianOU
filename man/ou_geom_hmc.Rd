% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geometry.R
\name{ou_geom_hmc}
\alias{ou_geom_hmc}
\title{Static Hamiltonian Monte Carlo with a pluggable geometry}
\usage{
ou_geom_hmc(
  target,
  metric = NULL,
  epsilon = 0.1,
  L = 20L,
  n_iter = 1000L,
  n_warmup = 500L,
  init = NULL,
  seed = NULL,
  fp_tol = 1e-09,
  fp_max = 100L
)
}
\arguments{
\item{target}{A \code{\link{ou_geom_target}} (or object accepted by it).}

\item{metric}{A \code{\link{ou_geom_metric_euclidean}} /
\code{\link{ou_geom_metric_riemannian}}. Defaults to the identity metric.}

\item{epsilon}{Leapfrog step size.}

\item{L}{Leapfrog steps per proposal.}

\item{n_iter}{Retained iterations.}

\item{n_warmup}{Burn-in iterations discarded (no adaptation).}

\item{init}{Optional initial position (defaults to zeros).}

\item{seed}{Optional integer seed (RNG state set and restored).}

\item{fp_tol, fp_max}{Fixed-point tolerance / max iterations for the implicit
leapfrog (position-dependent metric only).}
}
\value{
A list of class \code{ou_geom_hmc} with \code{draws}, \code{accept_rate},
  \code{n_divergent}, \code{energy}, \code{ebfmi}, \code{epsilon}, \code{L} and
  \code{metric_type}.
}
\description{
Fixed step-size / fixed trajectory-length HMC with a Metropolis correction
over a pluggable metric. The default Euclidean metric is textbook HMC; a
Riemannian metric reuses the same loop with the generalised implicit leapfrog
of Girolami & Calderhead (2011). The metric is a preconditioner only, so the
returned draws are exact regardless of which metric is chosen.
}
